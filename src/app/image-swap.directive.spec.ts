import { ImageSwapDirective } from './image-swap.directive';

describe('ImageSwapDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageSwapDirective();
    expect(directive).toBeTruthy();
  });
});
